export enum EventType{
    loading = 'loading',
    error = 'error',
    navigate = 'nav',
    toast = 'toast'
}

export enum LoadingAction{
    show = 'show',
    hide = 'hide',
    hideAll = 'hide-all'
}