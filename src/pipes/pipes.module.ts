import { NgModule } from '@angular/core';
import { FromUnixPipe } from './from-unix/from-unix';
@NgModule({
	declarations: [FromUnixPipe],
	imports: [],
	exports: [FromUnixPipe]
})
export class PipesModule {}
